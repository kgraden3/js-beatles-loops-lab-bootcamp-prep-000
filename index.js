// add solution here
function theBeatlesPlay(musician, instrument){
  var theBeatles = [];
  for (let i = 0; i++) {
    return theBeatles.push(`${musician} plays ${instrument}`);
  }
  return theBeatles;
}
